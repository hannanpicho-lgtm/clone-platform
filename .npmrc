# NPM Configuration for Vercel
prefer-offline=false
prefer-online=true
registry=https://registry.npmjs.org/

# Ignore scripts to avoid permission issues
ignore-scripts=false

# Use npm ci instead of npm install on Vercel for deterministic builds
legacy-peer-deps=true
