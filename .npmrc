# NPM Configuration for Cloudflare Pages
prefer-offline=false
prefer-online=true
registry=https://registry.npmjs.org/

# Ignore scripts to avoid permission issues
ignore-scripts=false

# Use npm ci instead of npm install on Cloudflare Pages for deterministic builds
legacy-peer-deps=true
