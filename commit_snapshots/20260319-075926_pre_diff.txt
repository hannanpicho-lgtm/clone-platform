diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 0b48a024..c86e71d4 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -4,201 +4,164 @@ on:
   push:
     branches:
       - main
-      - production
   workflow_dispatch:
 
+concurrency:
+  group: production-deploy
+  cancel-in-progress: true
+
 jobs:
-  test:
-    name: Test & Validate
+  validate-secrets:
+    name: Validate Required Secrets
+    runs-on: ubuntu-latest
+    steps:
+      - name: Validate deployment secrets
+        env:
+          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
+          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
+          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
+          FUNCTION_URL: ${{ secrets.FUNCTION_URL }}
+          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
+          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
+          CLOUDFLARE_PAGES_PROJECT: ${{ secrets.CLOUDFLARE_PAGES_PROJECT }}
+        run: |
+          set -e
+          missing=""
+
+          check_secret() {
+            key="$1"
+            value="$2"
+            if [ -z "$value" ]; then
+              echo "::error title=Missing Secret::$key is not configured"
+              missing="1"
+            else
+              echo "Ô£à $key is configured"
+            fi
+          }
+
+          check_secret "SUPABASE_ACCESS_TOKEN" "$SUPABASE_ACCESS_TOKEN"
+          check_secret "SUPABASE_PROJECT_REF" "$SUPABASE_PROJECT_REF"
+          check_secret "SUPABASE_ANON_KEY" "$SUPABASE_ANON_KEY"
+          check_secret "FUNCTION_URL" "$FUNCTION_URL"
+          check_secret "CLOUDFLARE_API_TOKEN" "$CLOUDFLARE_API_TOKEN"
+          check_secret "CLOUDFLARE_ACCOUNT_ID" "$CLOUDFLARE_ACCOUNT_ID"
+          check_secret "CLOUDFLARE_PAGES_PROJECT" "$CLOUDFLARE_PAGES_PROJECT"
+
+          if [ -n "$missing" ]; then
+            echo "ÔØî Deployment secrets validation failed"
+            exit 1
+          fi
+
+          echo "Ô£à All required deployment secrets are configured"
+
+  test-and-build:
+    name: Test and Build
     runs-on: ubuntu-latest
-    
+    needs: validate-secrets
     steps:
-      - uses: actions/checkout@v3
-      
+      - uses: actions/checkout@v4
+
       - name: Setup Node.js
-        uses: actions/setup-node@v3
+        uses: actions/setup-node@v4
         with:
-          node-version: '18'
-          cache: 'npm'
-      
+          node-version: 20
+          cache: npm
+
       - name: Install dependencies
         run: npm ci
-      
+
       - name: Lint
         run: npm run lint --if-present
         continue-on-error: true
-      
-      - name: Run tests
-        run: npm run test:smoke
+
+      - name: Run regression guards
+        run: npm run test:regression-guards
+
+      - name: Run smoke tests (pre-deploy)
         env:
           SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
           FUNCTION_URL: ${{ secrets.FUNCTION_URL }}
+        run: npm run test:smoke
 
-      - name: Run regression guards
-        run: npm run test:regression-guards
-  
-  build:
-    name: Build Artifacts
-    runs-on: ubuntu-latest
-    needs: test
-    
-    steps:
-      - uses: actions/checkout@v3
-      
-      - name: Setup Node.js
-        uses: actions/setup-node@v3
-        with:
-          node-version: '18'
-          cache: 'npm'
-      
-      - name: Install dependencies
-        run: npm ci
-      
       - name: Build frontend
         run: npm run build
-      
-      - name: Upload build artifacts
-        uses: actions/upload-artifact@v3
+
+      - name: Upload build artifact
+        uses: actions/upload-artifact@v4
         with:
           name: dist
           path: dist/
-          retention-days: 5
-  
+          retention-days: 3
+
   deploy:
-    name: Deploy to Production
+    name: Deploy Frontend and Supabase
     runs-on: ubuntu-latest
-    needs: [test, build]
-    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
-    
+    needs: test-and-build
     steps:
-      - uses: actions/checkout@v3
-      
+      - uses: actions/checkout@v4
+
       - name: Setup Node.js
-        uses: actions/setup-node@v3
+        uses: actions/setup-node@v4
         with:
-          node-version: '18'
-      
-      - name: Install dependencies
-        run: npm ci
-      
-      - name: Setup Supabase CLI
+          node-version: 20
+
+      - name: Download build artifact
+        uses: actions/download-artifact@v4
+        with:
+          name: dist
+          path: dist
+
+      - name: Verify build output
         run: |
-          npm install -g supabase
-          supabase --version
-      
-      - name: Deploy Edge Function
-        run: npx supabase functions deploy make-server-44a642d3 --no-verify-jwt
+          if [ ! -f dist/index.html ]; then
+            echo "::error title=Build Artifact Missing::dist/index.html was not found"
+            exit 1
+          fi
+
+      - name: Install CLIs
+        run: npm install -g supabase wrangler
+
+      - name: Deploy to Cloudflare Pages
+        env:
+          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
+          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
+          CLOUDFLARE_PAGES_PROJECT: ${{ secrets.CLOUDFLARE_PAGES_PROJECT }}
+        run: |
+          wrangler pages deploy dist --project-name "$CLOUDFLARE_PAGES_PROJECT" --branch main
+
+      - name: Deploy Supabase function
+        env:
+          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
+          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
+        run: |
+          supabase functions deploy make-server-44a642d3 --project-ref "$SUPABASE_PROJECT_REF" --no-verify-jwt
+
+      - name: Apply DB schema
         env:
           SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
-          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
-      
-      - name: Health Check
+          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
+        run: |
+          supabase db query --project-ref "$SUPABASE_PROJECT_REF" --file supabase/schema/kv_store_44a642d3.sql
+
+      - name: Health check
+        env:
+          FUNCTION_URL: ${{ secrets.FUNCTION_URL }}
         run: |
-          FUNCTION_URL="${{ secrets.FUNCTION_URL }}"
-          echo "Checking health at: $FUNCTION_URL"
-          
-          for i in {1..5}; do
+          set -e
+          echo "Checking: $FUNCTION_URL/health"
+          for i in 1 2 3 4 5; do
             if curl -f "$FUNCTION_URL/health" > /dev/null 2>&1; then
               echo "Ô£à Health check passed"
               exit 0
             fi
-            echo "Attempt $i/5: Health check failed, retrying in 10 seconds..."
+            echo "Attempt $i/5 failed; retrying in 10s"
             sleep 10
           done
-          
-          echo "ÔØî Health check failed after 5 attempts"
+          echo "::error title=Health Check Failed::Endpoint did not return healthy response"
           exit 1
-      
-      - name: Post-Deployment Tests
-        run: npm run test:smoke
+
+      - name: Smoke tests (post-deploy)
         env:
           SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
           FUNCTION_URL: ${{ secrets.FUNCTION_URL }}
-      
-      - name: Create Deployment
-        uses: actions/github-script@v6
-        if: success()
-        with:
-          script: |
-            const deployment = await github.rest.repos.createDeployment({
-              owner: context.repo.owner,
-              repo: context.repo.repo,
-              ref: context.ref,
-              environment: 'production',
-              description: 'Clone Platform',
-              auto_merge: false,
-              required_contexts: []
-            });
-            
-            await github.rest.repos.createDeploymentStatus({
-              owner: context.repo.owner,
-              repo: context.repo.repo,
-              deployment_id: deployment.data.id,
-              state: 'success',
-              description: 'Deployment succeeded',
-              environment_url: '${{ secrets.FUNCTION_URL }}'
-            });
-      
-      - name: Notify Slack (Success)
-        if: success()
-        uses: 8398a7/action-slack@v3
-        with:
-          status: ${{ job.status }}
-          text: 'Ô£à Production deployment successful'
-          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
-          fields: repo,message,commit,author,action,eventName,ref,workflow
-      
-      - name: Notify Slack (Failure)
-        if: failure()
-        uses: 8398a7/action-slack@v3
-        with:
-          status: ${{ job.status }}
-          text: 'ÔØî Production deployment failed'
-          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
-          fields: repo,message,commit,author,action,eventName,ref,workflow
-  
-  rollback:
-    name: Rollback (if Deploy Fails)
-    runs-on: ubuntu-latest
-    needs: deploy
-    if: failure()
-    
-    steps:
-      - uses: actions/checkout@v3
-        with:
-          fetch-depth: 2
-      
-      - name: Setup Node.js
-        uses: actions/setup-node@v3
-        with:
-          node-version: '18'
-      
-      - name: Check out previous version
-        run: |
-          git checkout HEAD~1 -- supabase/functions/server/index.tsx
-          echo "Rolled back to previous version"
-      
-      - name: Rebuild and redeploy
-        run: |
-          npm install
-          npm run build
-          npx supabase functions deploy make-server-44a642d3 --no-verify-jwt
-        env:
-          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
-          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
-      
-      - name: Notify Slack (Rollback)
-        uses: 8398a7/action-slack@v3
-        with:
-          status: custom
-          custom_payload: |
-            {
-              text: 'ÔÜá´©Å Production deployment failed - rolled back to previous version',
-              color: 'warning',
-              fields: [{
-                title: 'Repo',
-                value: context.payload.repository.full_name,
-                short: true
-              }]
-            }
-          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
+        run: npm run test:smoke
diff --git a/.github/workflows/supabase-deploy.yml b/.github/workflows/supabase-deploy.yml
deleted file mode 100644
index d6ab8e49..00000000
--- a/.github/workflows/supabase-deploy.yml
+++ /dev/null
@@ -1,42 +0,0 @@
-name: Supabase Deploy
-
-on:
-  push:
-    branches:
-      - main
-
-jobs:
-  deploy:
-    runs-on: ubuntu-latest
-    steps:
-      - uses: actions/checkout@v4
-
-      - name: Setup Node
-        uses: actions/setup-node@v4
-        with:
-          node-version: 20
-
-      - name: Install supabase CLI
-        run: npm install -g supabase
-
-      - name: Run regression guards
-        run: node scripts/regression-guards.mjs
-
-      - name: Deploy functions
-        env:
-          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
-          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
-        run: |
-          supabase functions deploy make-server-44a642d3 --project-ref $SUPABASE_PROJECT_REF --no-verify-jwt
-
-      - name: Apply DB schema
-        env:
-          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
-          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
-        run: |
-          supabase db query --project-ref $SUPABASE_PROJECT_REF --file supabase/schema/kv_store_44a642d3.sql
-
-      - name: Run smoke tests
-        run: |
-          node test-new-endpoints.js
-          node test-customer-service-endpoints.js
