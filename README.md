
  # Clone platform with backend

  This is a code bundle for Clone platform with backend. The original project is available at https://www.figma.com/design/zHhS66YmdqdtFR0fnJN8wR/Clone-platform-with-backend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  